name: Manual publish

on: workflow_dispatch

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  publish:
    name: Manual publish
    runs-on: ubuntu-latest
    steps:
      - name: Publish to npm
        uses: ./.github/actions/publish
        with:
          token: ${{ secrets.NPM_TOKEN }}
